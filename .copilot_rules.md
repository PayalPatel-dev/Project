# 📋 Copilot Rules - BITS Project

**Purpose:** Efficient project management with minimal token waste and clear library management.

---

## 📚 Library Management

### Rule 1: Always Update requirements.txt

- **Whenever you use a NEW library in the project**, add it to `requirements.txt`
- Add ONLY direct dependencies (not transitive dependencies)
- Use format: `package_name==version` or `package_name>=minimum_version`
- Keep requirements.txt minimal and clean - remove unused libraries
- Review requirements.txt quarterly and remove obsolete packages

### Current Project Dependencies

- **numpy** - Numerical computing
- **pandas** - Data manipulation
- **torch** - Deep learning (LSTM models)
- **datasets** - Load Hugging Face datasets
- **transformers** - NLP models & utilities
- **sentence-transformers** - Text embeddings
- **nltk** - NLP tokenization
- **scikit-learn** - ML metrics (AUROC, confusion matrix)
- **matplotlib** - Plotting & visualization
- **tqdm** - Progress bars
- **PyYAML** - Configuration file parsing

---

## 📋 Documentation Standards

### Rule 2: Keep Docs Minimal

- One README or QUICK_START guide maximum (not both)
- Remove duplicated documentation
- Archive old guides in `docs/archived/` if needed
- All docs should have clear purpose and be actively maintained
- NO separate .md files unless explicitly requested by user

### Current Essential Docs Only

- `MULTIMODAL_QUICK_START.md` - Main execution guide
- `ALL_FILES_COMPLETE.md` - Reference (archive if not used)

---

## 🔧 File Creation Rules

### Rule 3: File Organization by Type

- **Python scripts** (execution files) → Root directory (`*.py`)
- **Utility/helper scripts** → `scripts/` folder
- **Configuration files** → `config/` folder
- **Documentation** → `docs/` folder
- **Generated outputs** (results, models, data) → `logs/` or project-specific output folder
- **Never leave generated files in main directory** - organize by type immediately

### File Creation

- ❌ **DO NOT** create markdown summary files (\*.md) unless explicitly requested
- ❌ **DO NOT** create validation reports automatically
- ❌ **DO NOT** create side-by-side comparison files
- ❌ **DO NOT** leave any generated files in root directory - move to appropriate folder
- ✅ **DO** create files only when user explicitly asks for them
- ✅ **DO** edit existing code files when fixing issues
- ✅ **DO** provide inline code fixes directly without separate documentation
- ✅ **DO** organize all generated files into respective folders

### Output Format

- ❌ **DO NOT** generate lengthy multi-part validation reports
- ✅ **DO** provide concise summary in chat (1-2 paragraphs max)
- ✅ **DO** list findings in bullet points only when asked
- ✅ **DO** mention specific file paths and line numbers for reference

### Response Style

- Keep responses **short and direct**
- No verbose explanations unless asked for details
- No separate "detailed reports" unless requested
- Focus on **what was changed** and **why**, not extensive documentation

---

## 🔧 Code Modification Rules

### When Editing Code

1. Use `replace_string_in_file` tool directly
2. Don't create backup files
3. Don't generate "before/after" documentation
4. Notify user of changes made in a single sentence per fix

### For Python Files

- Fix deprecation warnings silently
- Update syntax without explanation unless critical
- Run validation only if user asks to test

### For Database/Data Issues

- Verify against actual schema from exploration logs
- Don't create extra validation files
- Report findings in the chat directly

---

## 🚫 Files to NOT Create (Unless Requested)

- `CODE_VALIDATION_REPORT.md` ❌
- `DATABASE_VALIDATION.md` ❌
- `VALIDATION_SUMMARY.txt` ❌
- `*.backup` or `*.old` ❌
- Comparison files ❌
- Summary worksheets ❌
- Analysis documents ❌

---

## ✅ Files OK to Create

- **Config files** (requirements.txt, setup.py, etc.)
- **Source code files** (_.py, _.js, etc.)
- **Data files** (outputs, models, etc.)
- **Only documented request files** (user explicitly asks for docs)

---

## 📊 Token-Saving Tactics

1. **Reuse existing documentation** - Refer to files like `mimic_exploration_20251213_122620.txt` already in project
2. **Chat-only responses** - Answer questions directly without creating files
3. **Quick fixes** - Edit and mention in one line: "Fixed pandas deprecation on line 158"
4. **Minimal explanation** - User can ask for details if needed

---

## 🎯 Example Responses

### ❌ BEFORE (Token waste)

```
I've analyzed your code and found 2 issues:
[Creates 3 markdown files with detailed reports]
[Generates comparison tables]
[Writes extensive validation documentation]
```

### ✅ AFTER (Token efficient)

```
Fixed 2 pandas deprecations:
- Line 158: Changed fillna(method='ffill') → .ffill()
- Line 225: Changed fillna(method='ffill') → .ffill()

Code is ready to run.
```

---

## 📝 Exceptions (When to Create Docs)

Create documentation **only** when user explicitly asks:

- "Create a README"
- "Write documentation"
- "Generate a report"
- "Make a summary file"
- "Document the process"

---

**Last Updated:** December 13, 2025  
**Applies To:** All interactions in BITS_Project workspace
